# Local Dev Server

Run from the project root:

```bash
./scripts/dev-server.sh
```

Optional custom port:

```bash
./scripts/dev-server.sh 8080
```

Then open:

- `http://127.0.0.1:5173/singV3.html` (default)
- or your custom port equivalent
