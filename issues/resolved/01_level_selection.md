Display level selection and number of exercises below each other
instead of a numeric input for levels let the user directly select the level (one button/field for each level)
of instead of a numeric input for number of exercise give the user 3 options:
5, 10, 20, unlimited
Unlimited means session ends only after user ends session manually, needs to be implemented